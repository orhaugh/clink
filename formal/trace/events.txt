# The protocol trace vocabulary (design record 012, increment 3).
#
# `event <Name>` lines are the events the engine may emit
# (protocol_trace::Event("<Name>") in the source tree); every one must be
# consumed by formal/trace/TraceExactlyOnce.tla (Is("<Name>")).
# `unobserved <Action>` lines are specification actions the engine cannot
# observe and so never emits; the trace module lets TLC take them as hidden
# steps. Every other action in ExactlyOnce.tla's Next must be reached by
# some event. `outside <Name>` lines are events that mark a run as outside
# the specification's scope: the engine emits them, the module never
# consumes them, and the validator skips a trace that carries one rather
# than reporting it as a divergence. scripts/check-protocol-trace-events.py
# holds all of it in agreement.
#
# Fields per event (beyond seq, ts, proc): see docs/internals/exactly-once-specification.md.
event Trigger
event DeliverBarrier
event SinkPrepare
event SubtaskAck
event CoordComplete
event WriteCompleted
event Broadcast
event DeliverCommit
event DeliverAbort
event SinkCommit
event SinkReceipt
event SinkConfirm
event WriteConfirmed
event WorkerDies
event RestartOnError
event SubtaskDrained
event CoordRecovers
event RestartProceeds
event Redeploy
event WalkSkips
event WalkReadsReceipt
event WalkProbes
event WalkRetries
event WalkExhausted
event WalkCancelled
event WalkDecides
event WalkFinishes
event SinkOpens
event Placement
unobserved CoordDies
unobserved CoordSuperseded
unobserved ZombieStops
unobserved TxnExpires
unobserved BrokerGoesDown
unobserved BrokerComesBack
unobserved Done
# A rescale renumbers and re-places the operator's subtasks. The model keys a
# sink by its subtask index and fixes the set and its hosts for the run, so a
# rescaled run is outside it until the model covers rescale.
outside Rescale
