-- Nexmark q1 on flink, BLACKHOLE sink variant. Currency conversion. Per-
-- record projection with a float multiply.
-- GENERATED by queries/gen_queries.py from the single cross-engine
-- definition. Edit that file, not this one.
CREATE TABLE bid (
  `auction` BIGINT, `bidder` BIGINT, `price` BIGINT, `channel` STRING, `url` STRING, `datetime` BIGINT,
  ts AS TO_TIMESTAMP_LTZ(`datetime`, 3),
  WATERMARK FOR ts AS ts - INTERVAL '4' SECOND
) WITH (
  'connector' = 'kafka',
  'topic' = 'nx-bid',
  'properties.bootstrap.servers' = 'kafka:29092',
  'properties.group.id' = 'flink-q1bh-bid',
  'scan.startup.mode' = 'earliest-offset',
  'format' = 'json',
  'json.ignore-parse-errors' = 'false'
);
CREATE TABLE sink_q1 (`auction` BIGINT, `bidder` BIGINT, `price` DOUBLE, `datetime` BIGINT) WITH ('connector' = 'blackhole');
INSERT INTO sink_q1
SELECT auction, bidder, price * 0.908, `datetime` FROM bid;
